diff --git a/backend/accessproj/settings/base.py b/backend/accessproj/settings/base.py
index f75c4a8..4e93e20 100644
--- a/backend/accessproj/settings/base.py
+++ b/backend/accessproj/settings/base.py
@@ -15,0 +16 @@ INSTALLED_APPS = [
+    "corsheaders",
@@ -17,0 +19 @@ INSTALLED_APPS = [
+    "drf_spectacular",
@@ -25,0 +28,3 @@ MIDDLEWARE = [
+    "core.middleware.RequestIdMiddleware",
+    "core.middleware.AccessLogMiddleware",
+    "corsheaders.middleware.CorsMiddleware",
@@ -60,0 +66 @@ DATABASES = {
+        "CONN_MAX_AGE": 60,
@@ -90 +96 @@ REST_FRAMEWORK = {
-        "rest_framework.permissions.AllowAny",
+        "rest_framework.permissions.IsAuthenticated",
@@ -102 +108,19 @@ REST_FRAMEWORK = {
-}
\ No newline at end of file
+    "DEFAULT_SCHEMA_CLASS": "drf_spectacular.openapi.AutoSchema",
+}
+
+SPECTACULAR_SETTINGS = {
+    "TITLE": "OpenWay Access API",
+    "VERSION": "1.0.0",
+    "SERVE_INCLUDE_SCHEMA": False,
+    "SERVE_PERMISSIONS": ["rest_framework.permissions.AllowAny"],
+}
+
+# CORS settings
+CORS_ALLOWED_ORIGINS = os.environ.get(
+    "CORS_ALLOWED_ORIGINS",
+    "http://localhost:3000,http://localhost:8080"
+).split(",")
+CORS_ALLOW_CREDENTIALS = True
+
+# Import JSON logging configuration
+from .logging_json import LOGGING
